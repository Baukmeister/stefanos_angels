from dash import html

data_viz_html = html.Div([
    html.H3("DATA VIZ"),
    html.Span("This is where the data viz will go")
])
model_viz_html = html.Div([
    html.H3("MODEL VIZ"),
    html.Span("This is where the ML-Model viz will go")
])